{% if data.images.first %}
    {% set path = data.images.first.path|imagine_filter(filter|default('sylius_small')) %}
{% else %}
    {% set path = '//placehold.it/50x50' %}
{% endif %}

<img src="{{ path }}" alt="" class="ui bordered image" />
